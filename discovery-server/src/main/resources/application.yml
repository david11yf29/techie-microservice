spring:
  application:
    name: discovery-server

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

#Actuator Prometheus
management:
  endpoints:
    web:
      exposure:
        include: prometheus

server:
  port: 8761
